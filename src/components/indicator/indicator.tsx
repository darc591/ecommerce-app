import { AlertProps } from '@mui/material';
import { StyledIndicator } from './indicator.styles';

const Indicator = (props: AlertProps) => {
  return <StyledIndicator {...props} />;
};

export default Indicator;
